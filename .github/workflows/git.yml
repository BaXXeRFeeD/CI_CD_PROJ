name: GitHub Actions labwork
on:
  push:
    branches: [ "branch" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: cmake
      run: cmake -S, -B ./build/
    - name: make
      run: make
    - name: test
      run: test
